services:
  warehouse-a:
    environment:
      SCENARIO_DATA_PATH: /app/scenarios/same-version-divergence/warehouse-a.json

  warehouse-b:
    environment:
      SCENARIO_DATA_PATH: /app/scenarios/same-version-divergence/warehouse-b.json

  warehouse-c:
    environment:
      SCENARIO_DATA_PATH: /app/scenarios/same-version-divergence/warehouse-c.json
